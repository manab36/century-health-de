[project]
name = "century-health"
version = "0.1.0"
description = "Century Health data engineering take-home pipeline"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0,<3.0",
    "pyarrow>=12.0",
    "openpyxl>=3.1",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
]
